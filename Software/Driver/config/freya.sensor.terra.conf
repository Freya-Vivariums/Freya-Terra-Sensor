<!-- This configuration file specifies the required security policies
     for the freya.sensor.terra service to work -->

<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
 
<busconfig>
    <!-- Root policy -->
    <policy user="root">
        <allow own="freya.sensor.terra"/>
        <allow send_destination="freya.sensor.terra"/>
        <allow receive_sender="freya.sensor.terra"/>
    </policy>
</busconfig>
